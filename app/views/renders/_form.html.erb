<%= form_for(@render) do |f| %>
  <% if @render.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@render.errors.count, "error") %> prohibited this render from being saved:</h2>

      <ul>
      <% @render.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
